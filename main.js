function solution() {
  // define the DOM elements and store them in different variables
  // validate the DOM elements "first_name" and "last_name" to contain some character, they can't be empty
  // validate the DOM element "email" to contain one @ character, before and after have to contain letters and numbers and dots. Cannot be empty
  // validate the if company is checked, then validate the elements "company_name" (cannot be empty) and "phone" (it can't be empty consisting of at least six digits, can contain dashes and spaces)
}

// write your code in JavaScript (Node.js 6.4.0)
//
// you can access DOM Tree using DOM Object Model:
//    document.getElementById
// or using jQuery:
//    $('some_tag')
//
// you can write to stdout for debugging purposes, e.g.
// console.log('this is a debug message');
